rules:
  - description: nostdinc found
    rule: '[opt for opt in copts if "nostdinc" in opt] and libcxx == true'

  - description: nostdlib found
    rule: '[opt for opt in linkopts if "-nostdlib" in opt] and libcxx == true'

